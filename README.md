To run the app, got to assignment folder and run following commands:

1. `npm install`
2. `npm start`


Right now logout functionality hasn't been implemented, so please clear cookies from browser to run the app again.